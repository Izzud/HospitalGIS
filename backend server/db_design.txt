CREATE TABLE hospital(
   ID SERIAL PRIMARY KEY,
   NAME TEXT,
   STATE CHAR(3),
   BEDS INT,
   COVID_CASES INT,
   LONGITUDE FLOAT NOT NULL,
   LATITUDE FLOAT NOT NULL
);

CREATE TABLE app_user(id int generated always as identity ( cache 10 ) primary key, username text unique not null, password text);